# Handoff — 2026-05-25

## In flight
- Nothing actively in progress in this repo. All Friday's commits pushed to `origin/main` (twilsher/cookidoo-mcp). 7 ahead of `upstream/main` (rupawel).

## Blocked / waiting
- **Upstream PR #1** (Nordic locales, https://github.com/rupawel/cookidoo-mcp/pull/1): open since 2026-05-16, no maintainer activity. Not chasing.
- **cookidoo-api PR #203** (browser OAuth2): still open. Our pin is to commit `ad9fb13` (head of `new-auth` branch). If merged → swap to released version (likely 0.18.0+). If force-pushed → our install may break; pin is to the SHA, not the branch name, so the worst case is a "ref not found" error, not silent breakage.

## Tomorrow first action
**Fix the MCP session 401 / no-refresh-path bug.** Live MCP server returns 401 from `cookidoo.international/planning/...` after the session cookies expire (currently ~daily). The new-auth library has no refresh; the user has to `/mcp reconnect cookidoo` every morning. Plan: wrap `client.api()` so that if any subsequent call raises `aiohttp.ClientResponseError(status=401)`, we drop the session, re-login, and retry once. Touch only `src/cookidoo_mcp/client.py`. Verify by waiting out cookie expiry (or forcing a 401) and confirming auto-recovery.

## Time-sensitive
- **Wed 5/27 dinner:** Halloumi- og avokadopita (`01KS7SC3BNGA3FTF0ZNDZNZQR9`) — custom recipe, only visible via MCP read fix (commit 89f4ae6). Already on calendar.
- **Thu 5/28 dinner:** Easy mac and cheese (r622110) — added since Friday.
- **Wed 6/3:** Sous-vide Korean-style BBQ Pork Loin — long cook, plan ahead.

## Notes
- **From-scratch Thermomix integration:** paused since 2026-05-22. Awaiting kick-off from Thomas. Context: both upstream forks are problematic (Mariosd23 has create/edit but stale auth; miaucl/new-auth has working auth but no create/edit), so we own a raw-HTTP escape hatch (cookidoo-mcp `cookidoo_http_request` tool) and have an inlined create-custom-recipe path in `/tmp/halloumi_create.py` that worked. A proper integration would consolidate these into one library.
- **Plaintext password in transcript:** earlier in this session a `bash -x` trace dumped the Cookidoo password into the transcript log. Thomas said he'd rotate it; status unknown. If rotation hasn't happened, `~/.secrets/cookidoo` is unchanged.
- **No background processes / browser sessions** owned by this window.
